package collectionFramework.Problems.linkedList;

public class QuickSortList {
    public static Node quickSort(Node head) {
        if(head==null || head.next==null) return head;
        // Simplified placeholder for QuickSort logic
        return head;
    }
}